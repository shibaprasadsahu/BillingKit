name: CI

on:
  push:
    branches: [ main, dev ]
  pull_request:
    branches: [ main ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  checks: write
  pull-requests: write

jobs:
  lint:
    name: Lint Check
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.2

      - name: Set up JDK 21
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run Lint
        run: ./gradlew lint

      - name: Upload lint report
        uses: actions/upload-artifact@v7.0.1
        if: always()
        with:
          name: lint-report
          path: billingkit/build/reports/lint-results*.html

  test:
    name: Unit Tests
    runs-on: macos-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.2

      - name: Set up JDK 21
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run unit tests
        run: ./gradlew test --stacktrace

      - name: Upload test reports
        uses: actions/upload-artifact@v7.0.1
        if: always()
        with:
          name: test-reports
          path: |
            billingkit/build/reports/tests/
            billingkit/build/test-results/

      - name: Publish test results
        uses: EnricoMi/publish-unit-test-result-action/composite@v2.22.0
        if: always()
        with:
          files: |
            billingkit/build/test-results/**/*.xml

  build:
    name: Build Library
    runs-on: macos-latest
    needs: [lint, test]

    steps:
      - name: Checkout code
        uses: actions/checkout@v6.0.2

      - name: Set up JDK 21
        uses: actions/setup-java@v5.2.0
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: gradle

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Build library
        run: ./gradlew :billingkit:assembleRelease

      - name: Upload build artifacts
        uses: actions/upload-artifact@v7.0.1
        with:
          name: library-aar
          path: billingkit/build/outputs/aar/*.aar
